spring.application.name=progress-service
server.port=${SERVER_PORT:8083}

spring.data.mongodb.uri=mongodb://${MONGO_USER:admin}:${MONGO_PASSWORD:admin}@${MONGO_HOST:localhost}:${MONGO_PORT:27017}/playbook_db?authSource=admin

gamification.service.url=${GAMIFICATION_SERVICE_URL:http://localhost:8084}
book.service.url=${BOOK_SERVICE_URL:http://localhost:8082}